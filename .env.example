# Copy to .env if needed. Do NOT put Master Passphrase here.
PORT=3000
HOST=127.0.0.1
DATABASE_PATH=./data/vault.db
# AUTHZ_MODE=ownership | placeholder
# Use ownership for normal runs. placeholder fails closed (for contract tests of holder).
AUTHZ_MODE=ownership
